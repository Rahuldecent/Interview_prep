// Regular function -->>>arguments keyword can be used to access the passed argument when the function is invoked.
//arrow function --->>>>Arrow functions don't have their own arguments and it uses the argument from the outer function. 

// function regularFunction() {
//   console.log(arguments)
// }
// regularFunction(1, 2, 3); // Output: [Arguments] { '0': 1, '1': 2, '2': 3 }

